export const metadata = {
  title: "Blog Admin — Ina J Education",
  description: "Manage blog content",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
